version: '3.8'

services:
  redis:
    image: redis:7-alpine
    container_name: fabriq-redis
    restart: always
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - fabriq-network

volumes:
  redis_data:

networks:
  fabriq-network:
    driver: bridge